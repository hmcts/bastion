steps:
- task: qetza.replacetokens.replacetokens-task.replacetokens@3
  displayName: 'Replace tokens in **/*.*'
  inputs:
    targetFiles: '$(System.DefaultWorkingDirectory)/*.tf'
    tokenPrefix: '__'
    tokenSuffix: '__'