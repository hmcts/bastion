---
- name: Ansible Host
  hosts: 127.0.0.1
  connection: local
  gather_facts: false
  become: true
  tasks:
  - name: ansible create directory example
    file:
      path: /tmp/devops_directory
      state: directory