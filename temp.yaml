parameters:
  - name: pool
    type: string
    default: 'hmcts-sds-ptlsbox-windows'
    values:
      - 'hmcts-sds-ptlsbox-windows'
      - 'hmcts-sds-ptlsbox'

pool:
  name: ${{ parameters.pool }}

steps:
- script: echo "Keeping pool warm"
  displayName: 'Heartbeat'